# ==============================
# LUNA OS v3.0 — Requirements
# Stack Soberana MCT (Blindada)
# ==============================
# DEBT #M8/B8: Dependências otimizadas - removidas não usadas
# ==============================

# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic-core==2.14.1

# Rate Limiting
slowapi

# Supabase (client) - versão compatível
supabase==2.0.3

# AI / LLM
anthropic
openai

# HTTP Client (FIX: versão compatível com supabase_auth)
httpx==0.24.1
httpcore==0.17.3
aiohttp
httpx-sse==0.3.1
requests==2.31.0

# Utilities
loguru
pandas==2.1.3
python-dateutil
pytz
schedule

# File Processing (DEBT #M8: Comentado - usado apenas em scripts específicos)
# openpyxl       # Excel
# python-docx    # Word
# PyPDF2         # PDF

# ===== NOVA STACK v3.0 =====

# 1. Fila & Jobs (RQ + Redis)
redis==5.0.0
rq==1.14.1
rq-scheduler==0.13.1

# 2. Vector Database (Milvus)
pymilvus==2.3.6

# 3. Observabilidade (Jaeger / OpenTelemetry)
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-exporter-jaeger==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0
opentelemetry-instrumentation-redis==0.42b0
opentelemetry-instrumentation-requests==0.42b0
opentelemetry-instrumentation-sqlalchemy==0.42b0

# 4. Machine Learning (Churn Prediction) - DEBT #11 UPDATED
scikit-learn==1.5.0
xgboost==2.1.0
numpy==1.26.4
scipy==1.13.1
joblib==1.4.2
threadpoolctl==3.5.0

# 5. Analytics & Dashboards
# plotly==5.18.0  # REMOVIDO: Não importado no código principal

# 6. Testing & Quality
pytest==7.4.3
pytest-asyncio==0.21.1

# 7. Bugfixes & Compatibilidade
marshmallow<4.0.0,>=3.0.0

# ==============================
# REQUISITOS PARA DEV (Opcional)
# Instalar: pip install -r requirements-dev.txt
# ==============================
# openpyxl       # Scripts de Excel
# python-docx    # Scripts de Word
# PyPDF2         # Scripts de PDF
# plotly         # Dashboards
# pytest==7.4.3
# pytest-asyncio==0.21.1
